'use babel';

import AtomSelectNpmInstallView from '../lib/atom-select-npm-install-view';

describe('AtomSelectNpmInstallView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
